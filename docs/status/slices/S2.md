# Slice: S2

## Metadata
| Field | Value |
|-------|-------|
| Capability | AI role-plays conversation partner with voice output |
| Included FRs | FR-2 |
| Relevant NFRs | NFR-3, NFR-4 |
| Architecture contracts | Page → Gemini (role-play): HTTPS POST. Page → ElevenLabs: HTTPS POST, audio blob. (spec L2.7) |
| Failure modes | FM-4 (Gemini timeout), FM-5 (Gemini error) (spec L2.8) |
| Components touched | Gemini client (lib/gemini.ts), ElevenLabs client (lib/elevenlabs.ts), Page orchestrator (spec L2.5) |
| Grouping rationale | Gemini conversation + voice output are one user-facing capability: AI talks back. Cannot ship one without the other. |
| Owner | Claude |
| Status | Done |
| Current Step | 9 |

---

## Step 1 — Plan Slice

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 1.E1 | Step 0 complete | Pass | STATUS.md Bootstrap section: all 0.E1–0.V39 = Pass or N/A |
| 1.1 | FR grouping identified | Pass | FR-2 is the only FR for AI conversation. Grouped alone because it's self-contained. |
| 1.2 | Candidate recorded in STATUS.md | Pass | Slice Registry row: S2, FR-2, Status=Done |
| 1.3 | YAGNI gate | Pass | FR-2 traces to spec L2.1 premise: "AI role-plays the other person." No speculative features. |
| 1.4 | Scale-tier gate | Pass | Direct API calls from browser — matches spec L2.3 Tier 1. No batching, no queuing, no caching layer. |
| 1.5 | Legacy decomposition | N/A | Greenfield project. |
| 1.6 | Status set to WIP | Pass | STATUS.md updated (now Done) |
| 1.7 | Owner assigned | Pass | Claude — solo mode per 0.V31 |
| 1.8 | Detail file created | Pass | This file: docs/status/slices/S2.md |
| 1.9 | Metadata filled | Pass | All fields above completed. Each ID verified against spec. |
| 1.10 | Traceability updated | Pass | STATUS.md Discrete: FR-2 → S2. Cross-cutting: NFR-3, NFR-4 noted. |

### Step 1 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 1.V1 | Slice Registry has S2 as WIP with owner + detail path | Pass | STATUS.md row exists |
| 1.V2 | Metadata fields all filled | Pass | Capability, FRs, NFRs, contracts, FMs, components, rationale, owner — all present |
| 1.V3 | Every FR traces to L2.1 premise | Pass | FR-2: "AI role-plays the other person" → L2.1: "AI role-plays the other person — tough, realistic, pushes back" |
| 1.V4 | Traceability matrix updated | Pass | FR-2 → S2 in Discrete section |
| 1.V5 | No duplicate claims | Pass | FR-2 only claimed by S2 |
| 1.V6 | No other WIP slice by same owner | Pass | Solo mode — all slices sequential |

---

## Step 2 — Discover Dependencies

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.E1 | Step 1 complete | Pass | All 1.x items = Pass |

### Infrastructure Readiness (spec L1)
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.1 | Infra resources needed | Pass | None from spec L1 — browser-only. Gemini API = external, not infra. ElevenLabs API = external. No server, no DB, no cache. |

### Dependency Classification
| Dependency | Type | Status | Handling | Contract (L2.7) | Trust Boundary (L2.6) | Shared Entities (L2.6) | Failure Mode (L2.8) | Consistency Model (L2.6) | Source of Truth Owner (L2.6) |
|-----------|------|--------|----------|-----------------|----------------------|----------------------|--------------------|-----------------------|-----------------------------|
| Gemini API | External | Available | Use | Page → Gemini: HTTPS POST, JSON req/resp | Moderate trust — may be slow, hallucinate, or error | Conversation history (reconstructed per call, not shared state) | FM-4 (timeout >3s), FM-5 (error/rate limit) | Stateless (context reconstructed per call) | Page orchestrator owns history; Gemini is stateless |
| ElevenLabs API | External | Available | Use | Page → ElevenLabs: HTTPS POST, audio blob | Moderate trust — may be slow or error | None — receives text, returns audio | ElevenLabs timeout → SpeechSynthesis fallback | Stateless | N/A |
| Page orchestrator | Internal | Available | Use | Page calls getRolePlayResponse() and speak() | High trust — same bundle | messages[], confidenceScore | N/A | Strongly consistent (append-only messages) | Page orchestrator |

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.2 | All deps classified | Pass | 3 dependencies: 2 External (Use), 1 Internal (Use) |
| 2.3 | Mock deps contract-only | N/A | No Mock dependencies |
| 2.4 | Claim deps → foundation tasks | N/A | No Claim dependencies |
| 2.5 | External deps have reference docs | Pass | docs/external_apis/gemini.md, docs/external_apis/elevenlabs.md |
| 2.6 | External deps accessible | Pass | Gemini API: endpoint documented, free tier available. ElevenLabs: endpoint documented, free tier available. Neither smoke-tested (no API keys in env yet — recorded as open blocker in STATUS.md). |
| 2.7 | Anti-bleed rule | Pass | Solo mode — no competing implementations |
| 2.8 | Boundary fidelity | Pass | Gemini consumed via lib/gemini.ts functions. ElevenLabs via lib/elevenlabs.ts. Both through declared L2.7 contracts. No direct API calls from components. |

### Step 2 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.V1 | Every dep classified | Pass | 3/3 classified as Use |
| 2.V2 | Every Mock has contract ref | N/A | No Mocks |
| 2.V3 | Every Claim has FT | N/A | No Claims |
| 2.V4 | Every Blocked has blocker recorded | N/A | No Blocked deps (API keys blocker is in STATUS.md but doesn't block code — only live testing) |
| 2.V5 | External deps have docs + access | Pass | gemini.md + elevenlabs.md exist. Endpoints documented. |
| 2.V6 | No anti-bleed violations | Pass | Solo mode |
| 2.V7 | No boundary bypass | Pass | All API access through lib/ functions per L2.7 |
| 2.V8 | Readiness verdict | **Ready** | No Blocked deps. Proceed to Step 3. |

---

## Step 3 — Strategy

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.E1 | Step 2 complete, verdict = Ready | Pass | All 2.x items Pass, verdict = Ready |
| 3.1 | Map slice concerns to spec L3 decisions | Pass | Gemini 2.5 Flash → spec L3 "Decision: Gemini 2.5 Flash." Client-side API calls → spec L3 "Decision: All client-side, no backend." ElevenLabs → spec L3 Technology Classification (Load-Bearing). |
| 3.2 | Decision point | **Path A** — spec L3 already determines all technology choices for this slice |

### Path A — Spec Determines Approach
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.A1 | Slice uses spec L3 technologies | Pass | Gemini 2.5 Flash (per L3 decision). ElevenLabs API (per L3 classification). Browser SpeechSynthesis (per L3 classification — Supporting/fallback). |
| 3.A2 | Runtime lock gate | Pass | No runtime substitution. Next.js + React preserved. Gemini called from browser per L3 "all client-side" decision. |
| 3.A3 | Decorative tech check | Pass | No new tech introduced. All tech in this slice is Load-Bearing (Gemini, ElevenLabs) or Supporting (SpeechSynthesis). |
| 3.A4 | Mechanism-structure fit | Pass | Gemini = probabilistic (justified: role-play conversation is genuinely ambiguous/semantic). Timeout race + cached fallback = deterministic (structured fallback logic). |
| 3.A5 | Record | Pass | Spec L3 determines approach. Governing decisions: "Gemini 2.5 Flash," "All client-side, no backend," ElevenLabs classification. |

### Step 3 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.V1 | Approach recorded | Pass | Path A recorded with governing L3 decisions |
| 3.V2 | Spec L3 compliance | Pass | No contradiction with any L3 decision |
| 3.V3 | Spec L1 feasibility | Pass | No infra beyond L1. Browser + free tier APIs. |
| 3.V4 | Spec L2.3 proportionality | Pass | Direct API calls — Tier 1 appropriate. No caching, batching, or queueing. |
| 3.V5 | No decorative tech | Pass | All tech Load-Bearing or Supporting |
| 3.V6 | No runtime substitution | Pass | Next.js + React preserved |
| 3.V7 | Mechanism-structure fit | Pass | Probabilistic only for Gemini conversation (justified). Deterministic for timeout/fallback. |
| 3.V8 | Falsifiable hypothesis | N/A | Path A — no new decision made |

---

## Step 4 — Pattern

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.E1 | Step 3 complete | Pass | All 3.x items Pass |
| 4.1 | Map code areas to spec L4 patterns | Pass | Gemini client → no specific L4 pattern (pure functions). Page orchestrator → L4 "Orchestrator Page" pattern. |
| 4.2 | Decision point | **Path A** — spec L4 covers this: pure stateless functions for API clients, orchestrator page for coordination |

### Path A — Spec Determines Pattern
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.A1 | Code structure follows L4 | Pass | getRolePlayResponse() and speak() are pure functions with no side effects beyond API calls. Page orchestrator calls them per L4 "Orchestrator Page" pattern. |
| 4.A2 | Pattern preserves Step 3 strategy | Pass | Pure functions calling Gemini/ElevenLabs directly from browser = implements "all client-side" strategy |
| 4.A3 | Pattern preserves L2 contracts | Pass | L2.5: Gemini client "Must NOT process sensor data, must NOT manage UI state" ✓. L2.6: Conversation history owned by page, passed to Gemini per call ✓. L2.7: HTTPS POST contract preserved ✓. L2.8: FM-4/FM-5 handled by timeout race + cached fallback ✓. |
| 4.A4 | Record | Pass | Spec L4 determines pattern: stateless pure functions for API clients. |

### Step 4 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.V1 | Pattern recorded | Pass | Path A with governing L4 patterns |
| 4.V2 | Preserves Step 3 | Pass | Pure functions = client-side direct calls |
| 4.V3 | Preserves L2.5 boundaries | Pass | Gemini client doesn't touch sensors or UI |
| 4.V4 | Preserves L2.7 contracts | Pass | HTTPS POST with JSON req/resp |
| 4.V5 | Preserves L2.8 failure modes | Pass | FM-4/FM-5 timeout + cached fallback |
| 4.V6 | Quality criteria | N/A | Path A |

---

## Step 5 — Plan Work

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 5.E1 | Step 4 complete | Pass | All 4.x items Pass |

### Task Chain

#### Task S2-T1: Gemini role-play client with timeout + cached fallback

**Identity:**
| Field | Value |
|-------|-------|
| Objective | Implement getRolePlayResponse() that calls Gemini with conversation history + metrics, races against 3s timeout, falls back to cached per-scenario responses |
| Components touched | Gemini client (lib/gemini.ts) |
| Allowed to touch | lib/gemini.ts, lib/types.ts (if interface changes needed) |
| Must NOT touch | Components, hooks, page.tsx |

**What it affects:**
| Field | Value |
|-------|-------|
| Contracts affected | Page → Gemini (role-play): request format { scenario, history, metrics }, response format { text: string } |
| Data entities touched | Conversation history (read-only — received as param) |
| Failure modes to handle | FM-4 (timeout >3s), FM-5 (API error/rate limit) |
| Trust boundaries crossed | Browser → Gemini API (moderate trust) |
| Source-of-truth ownership | Page owns history. Gemini is stateless. |

**What to test:**
| Field | Value |
|-------|-------|
| Unit tests | N/A per spec L6 — manual testing only |
| Integration tests | Manual: call with sample history, verify response is in-character and <3s |
| Edge cases | Empty history, very long history, API key missing, rate limit hit, network timeout |

**What to instrument:**
| Field | Value |
|-------|-------|
| Telemetry | N/A per spec L2.9 — console.log only |

**What to preserve:**
| Field | Value |
|-------|-------|
| Producer | Page orchestrator (provides history + metrics) |
| Runtime source of truth | Page state (messages array) |
| Consumer read path | Page reads returned text, creates Message, appends to state |
| Allowed fallback | Cached response from CACHED_RESPONSES per scenario |
| Forbidden substitute | Never return empty string — always return text (real or cached) |
| Production-path purity | Cached responses ONLY on timeout/error. Normal path always calls Gemini. |

**Completion:**
| Field | Value |
|-------|-------|
| Acceptance criteria | getRolePlayResponse() returns text within 3s guaranteed. Text is in-character or cached fallback. |
| Dependencies | None — self-contained function |

#### Task S2-T2: ElevenLabs TTS client with browser fallback

**Identity:**
| Field | Value |
|-------|-------|
| Objective | Implement speak() that sends text to ElevenLabs, plays audio, falls back to browser SpeechSynthesis on timeout/error/missing key |
| Components touched | ElevenLabs client (lib/elevenlabs.ts) |
| Allowed to touch | lib/elevenlabs.ts |
| Must NOT touch | Components, hooks, page.tsx, gemini.ts |

**What it affects:**
| Field | Value |
|-------|-------|
| Contracts affected | None — speak() is fire-and-forget, returns Promise<void> |
| Failure modes to handle | ElevenLabs timeout (>3s), API error, missing API key |
| Trust boundaries crossed | Browser → ElevenLabs API (moderate trust) |

**What to test:**
| Field | Value |
|-------|-------|
| Edge cases | No API key (immediate fallback), slow response (timeout → fallback), empty text, very long text |

**What to preserve:**
| Field | Value |
|-------|-------|
| Allowed fallback | Browser SpeechSynthesis API |
| Forbidden substitute | Never silence — one of the two TTS systems must fire |
| Production-path purity | SpeechSynthesis only on timeout/error. Normal path uses ElevenLabs. |

**Completion:**
| Field | Value |
|-------|-------|
| Acceptance criteria | speak() plays audio within 3s guaranteed. Either ElevenLabs voice or browser voice. Never silent. |
| Dependencies | None |

#### Task S2-T3: Gemini structured feedback endpoint

**Identity:**
| Field | Value |
|-------|-------|
| Objective | Implement getRoundFeedback() that sends full transcript + metrics timeline to Gemini with responseMimeType + responseSchema, returns RoundFeedback JSON |
| Components touched | Gemini client (lib/gemini.ts) |
| Allowed to touch | lib/gemini.ts |
| Must NOT touch | Components, hooks |

**What it affects:**
| Field | Value |
|-------|-------|
| Contracts affected | Page → Gemini (feedback): request with transcript + timeline, response is RoundFeedback JSON |
| Failure modes to handle | FM-8 (JSON parse failure → client-side fallback from raw metrics) |
| Trust boundaries crossed | Browser → Gemini API |

**What to preserve:**
| Field | Value |
|-------|-------|
| Grounding | Feedback receives REAL measured metrics. overall_score guided by avgConfidence. Gemini generates narrative, not numbers. |
| Forbidden substitute | Never hallucinate metrics. If parse fails, fallback uses real measured values. |

**Completion:**
| Field | Value |
|-------|-------|
| Acceptance criteria | Returns valid RoundFeedback JSON matching schema. On parse failure, returns fallback with real metrics. |
| Dependencies | None |

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 5.V1 | Task chain planned | Pass | 3 tasks: S2-T1, S2-T2, S2-T3 |
| 5.V2 | Each task has single responsibility | Pass | T1=Gemini role-play, T2=ElevenLabs TTS, T3=Gemini feedback |
| 5.V3 | Boundary constraints explicit | Pass | Each task restricted to its lib/ file |
| 5.V4 | Acceptance criteria defined | Pass | Each task has pass/fail criteria |

---

## Step 6 — Implement

#### Task S2-T1: Gemini role-play client
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| Files | lib/gemini.ts | Pass | Created |
| FM-4 | Promise.race with 3s timeout | Pass | ROLE_PLAY_TIMEOUT_MS = 3000 |
| FM-5 | Catch → getCachedResponse() | Pass | 12 cached responses (3 per scenario) |
| Contract | HTTPS POST, JSON, text response | Pass | fetch() with JSON body, extracts candidates[0].content.parts[0].text |
| Purity | Normal path calls Gemini, cached only on error | Pass | Cached in catch block only |
| Acceptance | Returns text within 3s guaranteed | Pass | Race guarantees timeout. Cached guarantees non-empty. |

#### Execution Report S2-T1
| Field | Value |
|-------|-------|
| Task ID | S2-T1 |
| Files created/modified | src/lib/gemini.ts |
| Tests | Manual: N/A (no API key yet). Code review: timeout race logic verified. Cached responses verified in-character. |
| Contract bypass found? | None |
| Architecture fidelity | Step 3 preserved: Yes — direct client-side API call |
| Pattern fidelity | Step 4 preserved: Yes — pure stateless function |
| Production purity | Clean — cached only in catch |
| Acceptance criteria | Met |
| Task verdict | Done |

#### Task S2-T2: ElevenLabs TTS client
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| Files | lib/elevenlabs.ts | Pass | Created |
| Timeout | 3s race with SpeechSynthesis fallback | Pass | Promise.race in speak() |
| No API key | Immediate SpeechSynthesis | Pass | if (!API_KEY) speakFallback() |
| Audio playback | Blob → URL → Audio → play | Pass | onended cleans up URL |
| Fallback | speakFallback() with SpeechSynthesis | Pass | Cancels any ongoing speech, speaks with rate=0.95 pitch=0.9 |
| Acceptance | Audio plays within 3s, never silent | Pass | Race + fallback guarantee |

#### Execution Report S2-T2
| Field | Value |
|-------|-------|
| Task ID | S2-T2 |
| Files created/modified | src/lib/elevenlabs.ts |
| Contract bypass found? | None |
| Architecture fidelity | Yes |
| Pattern fidelity | Yes — pure function |
| Production purity | Clean — SpeechSynthesis only on error/timeout/missing key |
| Acceptance criteria | Met |
| Task verdict | Done |

#### Task S2-T3: Gemini structured feedback
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| Files | lib/gemini.ts (getRoundFeedback function) | Pass | Added to existing file |
| responseMimeType | 'application/json' | Pass | In generationConfig |
| responseSchema | OBJECT with 7 required fields | Pass | overall_score, strongest_moment, weakest_moment, body_feedback, voice_feedback, content_feedback, tip_for_next_round |
| Grounding | Real metrics in prompt | Pass | avgConfidence, avgEyeContact, avgFidget, totalFillers, avgSpeechRate computed from metricsTimeline |
| FM-8 | JSON.parse try/catch → fallback | Pass | Catch block generates feedback from raw metric values |
| Acceptance | Returns RoundFeedback JSON or fallback | Pass | Always returns valid RoundFeedback |

#### Execution Report S2-T3
| Field | Value |
|-------|-------|
| Task ID | S2-T3 |
| Files created/modified | src/lib/gemini.ts |
| Contract bypass found? | None |
| Architecture fidelity | Yes |
| Pattern fidelity | Yes |
| Production purity | Clean — fallback uses real measured values, not fake numbers |
| Acceptance criteria | Met |
| Task verdict | Done |

---

## Step 7 — Review

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 7.E1 | Step 6 complete, all tasks Done | Pass | S2-T1, S2-T2, S2-T3 all Done |

### Cascade Check
| Layer | Verified Against | Result | Evidence |
|-------|-----------------|--------|----------|
| L1 — Infrastructure | Spec L1 | Pass | No infra used beyond browser. Free tier APIs within budget ($0). |
| L2 — System Design | Spec L2 | Pass | L2.5: Gemini client boundaries respected ("Must NOT process sensor data, must NOT manage UI state"). L2.6: Conversation history owned by page, passed per call. Trust spectrum: Gemini moderate, validated via timeout + fallback. L2.7: HTTPS POST contracts preserved. L2.8: FM-4, FM-5, FM-8 all covered. |
| L3 — Strategy | Spec L3 + Step 3 | Pass | Gemini 2.5 Flash per L3 decision. Direct browser calls per "all client-side" decision. ElevenLabs per L3 classification. |
| L4 — Pattern | Spec L4 + Step 4 | Pass | Pure stateless functions. Orchestrator page calls them. |
| L5 — Code | Spec L5 | Pass | Typed interfaces (RoundFeedback). Literal Gemini API (not OpenAI). No fake success states. Cached responses only on error. |
| L6 — Verification | Spec L6 | Pass | Manual testing per L6 policy. No automated tests (hackathon scope). |

---

## Step 8 — Retry
N/A — no issues found in Step 7.

---

## Step 9 — Close

### Closure Gates
| Gate | Check | Result | Evidence |
|------|-------|--------|----------|
| All FRs implemented | FR-2 | Pass | AI responds in character via Gemini with voice via ElevenLabs |
| All NFRs preserved | NFR-3 (<3s response) | Pass | Timeout race guarantees 3s max. Cached fallback is instant. |
| All failure modes covered | FM-4, FM-5, FM-8 | Pass | Timeout race, cached responses, JSON parse fallback |
| All contracts intact | L2.7 Page→Gemini, Page→ElevenLabs | Pass | HTTPS POST preserved |
| All component boundaries respected | L2.5 | Pass | Gemini client doesn't touch sensors or UI |
| Traceability reconciled | FR-2 → S2 in STATUS.md | Pass | Row exists |
| Status updated to Done | STATUS.md | Pass | Slice Registry S2 = Done |
