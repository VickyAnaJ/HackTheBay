# Slice: S1

## Metadata
| Field | Value |
|-------|-------|
| Capability (user-visible outcome) | User selects a conversation scenario and controls round lifecycle (start, end, restart) |
| Included FRs (spec L2.4) | FR-1 (scenario selector with 3+ options), FR-14 (restart round with same scenario) |
| Relevant NFRs (spec L2.4) | NFR-4 (zero-instruction comprehension in <5s), NFR-6 (core flow ≤2 decisions) |
| Architecture contracts (spec L2.7) | None — this slice is UI-only with parent callbacks. No cross-boundary contracts. |
| Failure modes (spec L2.8) | None directly — no external dependencies, no API calls, no hardware access |
| Components touched (spec L2.5) | ScenarioSelector component, Page-level orchestrator (app/page.tsx) |
| Grouping rationale | Scenario selection and round lifecycle are the entry/exit points of the entire user flow. Without FR-1 (select scenario), no round can start. Without FR-14 (restart), the user can't practice again. They form one coherent capability: "navigate the app." |
| Owner | Claude |
| Status | Done |
| Current Step | 9 |

---

## Step 1 — Plan Slice

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 1.E1 | Step 0 complete (all 0.E1–0.V39 pass) | Pass | STATUS.md Bootstrap section: all items = Pass or N/A with justification. Verified 0.E1 (spec exists), 0.E2 (cascade check passes), 0.V1–0.V39 all recorded. |
| 1.1 | Identify candidate slices from spec L2.4 FRs | Pass | Read spec L2.4 FR table. FR-1 (scenario selector) and FR-14 (restart) share: same component (ScenarioSelector + page lifecycle), same user-facing outcome (navigating the app), no dependency on external APIs or sensors. Natural grouping. |
| 1.2 | Record candidate slices in STATUS.md Slice Registry | Pass | STATUS.md Slice Registry: S1, Capability="Scenario selection + round lifecycle", FRs="FR-1, FR-14", Status=Done |
| 1.3 | YAGNI gate | Pass | FR-1: spec L2.1 says "pick a scenario" — FR-1 implements exactly this. FR-14: spec L2.1 says "practice again → watch scores improve" — FR-14 implements the restart. No speculative features. No "custom scenario builder" or "save favorites" — just the 4 pre-built scenarios per spec. |
| 1.4 | Scale-tier gate | Pass | Spec L2.3: Tier 1 (1 DAU). This slice uses React useState for lifecycle state. No router, no persistence, no session management. Matches Tier 1 exactly. |
| 1.5 | Legacy decomposition gate | N/A | Greenfield project — no existing system to extract from. |
| 1.6 | Select one Planned slice, set Status = WIP | Pass | STATUS.md Slice Registry: S1 Status changed from Planned → WIP (now Done). |
| 1.7 | Assign owner | Pass | Claude — solo mode per STATUS.md 0.V31 declaration. |
| 1.8 | Create slice detail file | Pass | This file: docs/status/slices/S1.md. Copied structure from docs/status/_templates/SLICE_TEMPLATE.md. |
| 1.9 | Fill in slice metadata | Pass | All 9 metadata fields filled above. Each ID cross-checked: FR-1 exists in spec L2.4 (row 1), FR-14 exists in spec L2.4 (row 14), NFR-4 exists in spec L2.4 NFR table (row 4), NFR-6 exists (row 6), ScenarioSelector exists in spec L2.5 component registry, Page orchestrator exists in L2.5. |
| 1.10 | Update traceability matrix in STATUS.md | Pass | Discrete section: FR-1 Slice=S1, FR-14 Slice=S1. Cross-cutting: NFR-4 and NFR-6 noted as relevant (this slice must preserve zero-instruction UI and ≤2 decisions). Continuous: no continuous NFRs directly affected by this slice. No duplicate claims — FR-1 and FR-14 only appear in S1. |

### Step 1 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 1.V1 | Slice Registry has S1 as WIP with owner + detail path | Pass | STATUS.md row: S1, "Scenario selection + round lifecycle", FR-1/FR-14, Done, Step 9, Claude, docs/status/slices/S1.md |
| 1.V2 | Slice detail file exists with all metadata fields filled | Pass | This file. Capability, FRs (2), NFRs (2), contracts (none), failure modes (none), components (2), grouping rationale, owner — all present and non-empty. |
| 1.V3 | Every FR traces to spec L2.1 premise | Pass | FR-1: "User selects from pre-built conversation scenarios" → L2.1: "Pick a scenario." FR-14: "User can restart round with same scenario" → L2.1: "Practice again → watch scores improve round over round." Both trace directly. |
| 1.V4 | Traceability matrix updated for all claimed items | Pass | STATUS.md Discrete section: FR-1 Slice=S1, FR-14 Slice=S1. Both populated. |
| 1.V5 | No requirement claimed by two slices without justification | Pass | Scanned STATUS.md Discrete section — FR-1 and FR-14 only appear under S1. No duplicates. |
| 1.V6 | No other WIP slice owned by same person | Pass | Solo mode — all slices executed sequentially, one WIP at a time. S1 was WIP before S2 started. |

---

## Step 2 — Discover Dependencies

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.E1 | Step 1 complete (slice is WIP with full metadata) | Pass | All 1.E1–1.V6 items = Pass above |

### Infrastructure Readiness (spec L1)
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.1 | For each infra resource this slice needs: confirm provisioned in Step 0 | Pass | Components touched: ScenarioSelector (React component) + Page orchestrator (React component). Cross-referenced with spec L1: no infrastructure needed. Both are browser-rendered React components requiring only the Next.js dev server (provisioned in Step 0 via `npx create-next-app`). No database, no cache, no object storage, no messaging, no external APIs. |

### Dependency Classification
| Dependency | Type | Status | Handling | Contract (L2.7) | Trust Boundary (L2.6) | Shared Entities (L2.6) | Failure Mode (L2.8) | Consistency Model (L2.6) | Source of Truth Owner (L2.6) |
|-----------|------|--------|----------|-----------------|----------------------|----------------------|--------------------|-----------------------|-----------------------------|
| scenarios.ts (scenario data) | Internal | Available | Use | None — imported directly as module constant | High (same bundle) | Scenario[] array | None — static data, cannot fail | Immutable constant | lib/scenarios.ts |
| lib/types.ts (type definitions) | Internal | Available | Use | None — compile-time types only | High (same bundle) | RoundStatus, Scenario types | None — compile-time only | N/A | lib/types.ts |

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.2 | All deps classified | Pass | 2 dependencies: both Internal, both Use status. No Mock, Claim, or Blocked. |
| 2.3 | Every Mock has contract ref | N/A | No Mock dependencies |
| 2.4 | Every Claim has foundation task | N/A | No Claim dependencies |
| 2.5 | External deps have reference docs | N/A | No external dependencies in this slice |
| 2.6 | External deps accessible | N/A | No external dependencies |
| 2.7 | Anti-bleed rule | Pass | Solo mode — no competing implementations possible |
| 2.8 | Boundary fidelity | Pass | scenarios.ts consumed via ES module import. types.ts consumed via TypeScript import. Both are spec-declared modules. No direct DB access, no hardcoded values, no bypassed boundaries. |

### Step 2 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 2.V1 | Every dep classified | Pass | 2/2 classified as Use |
| 2.V2 | Every Mock has contract ref | N/A | No Mocks |
| 2.V3 | Every Claim has FT | N/A | No Claims |
| 2.V4 | Every Blocked has blocker recorded | N/A | No Blocked deps |
| 2.V5 | External deps have docs + access | N/A | No external deps |
| 2.V6 | No anti-bleed violations | Pass | Solo mode |
| 2.V7 | No boundary bypass | Pass | All deps consumed via module imports |
| 2.V8 | Readiness verdict | **Ready** | Zero dependencies with gaps. Proceed to Step 3. |

---

## Step 3 — Strategy

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.E1 | Step 2 complete, verdict = Ready | Pass | All 2.x items Pass/N/A, verdict = Ready |
| 3.1 | Map slice concerns to spec L3 decisions | Pass | ScenarioSelector component → spec L3: Next.js + React + TypeScript (Load-Bearing) + Tailwind CSS (Supporting). Page lifecycle → spec L3: React useState (part of React, Load-Bearing). No technology concern in this slice falls outside existing L3 decisions. |
| 3.2 | Decision point | **Path A** — spec L3 already determines all technology choices for this slice. No new decisions needed. |

### Path A — Spec Determines Approach
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.A1 | Slice uses spec L3 technologies | Pass | React components (L3: Load-Bearing). TypeScript interfaces (L3: Load-Bearing). Tailwind classes (L3: Supporting). No other tech used. |
| 3.A2 | Runtime lock gate | Pass | No runtime substitution. Next.js + React preserved. No Vue, no Svelte, no vanilla JS. |
| 3.A3 | Decorative tech check | Pass | No new tech introduced in this slice. Tailwind is Supporting per L3 classification — it does not control architecture. |
| 3.A4 | Mechanism-structure fit | Pass | All logic in this slice is deterministic: useState transitions (selecting → countdown → active → reviewing), setTimeout for countdown. No probabilistic mechanisms. Deterministic is correct — round lifecycle is structured/auditable work. |
| 3.A5 | Record | Pass | Spec L3 determines approach. No alternatives needed. Governing decisions: "Next.js + React + TypeScript" (Load-Bearing), "Tailwind CSS" (Supporting), "All client-side, no backend" (no server state for lifecycle). |

### Step 3 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 3.V1 | Approach recorded in slice log | Pass | Path A recorded with governing L3 decisions listed |
| 3.V2 | Spec L3 compliance | Pass | No contradiction. React + TypeScript + Tailwind per L3. |
| 3.V3 | Spec L1 feasibility | Pass | No infra beyond L1 needed. Browser-only. |
| 3.V4 | Spec L2.3 proportionality | Pass | React useState = simplest possible state management. Matches Tier 1. No Redux, no Zustand, no state machine library. |
| 3.V5 | No decorative tech | Pass | All tech is Load-Bearing (React, TS) or Supporting (Tailwind) |
| 3.V6 | No runtime substitution | Pass | Next.js + React preserved |
| 3.V7 | Mechanism-structure fit | Pass | All deterministic. No probabilistic mechanisms in lifecycle management. |
| 3.V8 | Falsifiable hypothesis | N/A | Path A — no new decision made, no hypothesis needed |

---

## Step 4 — Pattern

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.E1 | Step 3 complete | Pass | All 3.x items Pass/N/A |
| 4.1 | Map code areas to spec L4 patterns | Pass | Page lifecycle → spec L4: "Orchestrator Page" pattern ("app/page.tsx is the single coordination point that wires hooks to components, manages round lifecycle, and calls Gemini"). ScenarioSelector → spec L4: implicit dumb component (L4 says "All other components are dumb — they receive data and render"). |
| 4.2 | Decision point | **Path A** — spec L4 covers both code areas. No new pattern decisions needed. |

### Path A — Spec Determines Pattern
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.A1 | Code structure follows L4 | Pass | Page orchestrator: manages RoundStatus state machine (selecting → countdown → active → reviewing). ScenarioSelector: receives `onSelect` callback, renders cards, calls callback on click. Matches L4 "Orchestrator Page" + "dumb components" pattern. |
| 4.A2 | Pattern preserves Step 3 strategy | Pass | React useState for state management = implements the React strategy from Step 3. No conflict. |
| 4.A3 | Pattern preserves L2 contracts | Pass | L2.5: ScenarioSelector "Must NOT manage round state" ✓ — it only calls onSelect callback. Page orchestrator "is the only component allowed to call Gemini and coordinate state" ✓ — lifecycle state lives in page.tsx. L2.6: N/A for this slice (no data entities beyond local state). L2.7: no cross-boundary contracts in this slice. L2.8: no failure modes in this slice. |
| 4.A4 | Record | Pass | Spec L4 determines pattern. Governing patterns: "Orchestrator Page" for page.tsx, implicit dumb component for ScenarioSelector. |

### Step 4 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 4.V1 | Pattern recorded | Pass | Path A with L4 patterns listed |
| 4.V2 | Preserves Step 3 | Pass | React useState = React strategy |
| 4.V3 | Preserves L2.5 boundaries | Pass | ScenarioSelector doesn't manage state. Page manages lifecycle. |
| 4.V4 | Preserves L2.7 contracts | N/A | No cross-boundary contracts in this slice |
| 4.V5 | Preserves L2.8 failure modes | N/A | No failure modes in this slice |
| 4.V6 | Quality criteria | N/A | Path A — no new pattern decision |

---

## Step 5 — Plan Work

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 5.E1 | Step 4 complete | Pass | All 4.x items Pass/N/A |

### Task Chain

#### Task S1-T1: ScenarioSelector component

**Identity:**
| Field | Value |
|-------|-------|
| Objective | Render 4 scenario cards from scenarios.ts. Each card shows icon, title, description. Clicking a card calls onSelect callback with the selected Scenario object. |
| Components touched (spec L2.5) | ScenarioSelector |
| Allowed to touch | src/components/ScenarioSelector.tsx, src/lib/scenarios.ts |
| Must NOT touch | page.tsx, other components, hooks, other lib files |

**What it affects:**
| Field | Value |
|-------|-------|
| Contracts affected (spec L2.7) | None — internal component with callback prop |
| Data entities touched (spec L2.6) | Scenario[] (read-only import from scenarios.ts) |
| Failure modes to handle (spec L2.8) | None |
| Trust boundaries crossed (spec L2.6) | None — same bundle |
| Source-of-truth ownership to preserve (spec L2.6) | scenarios.ts owns scenario definitions |

**What to test:**
| Field | Value |
|-------|-------|
| Unit tests to write (spec L6) | N/A per spec L6 — manual testing only (hackathon) |
| Integration tests | Manual: verify 4 cards render, clicking each calls onSelect with correct scenario |
| Edge cases | Empty scenarios array (won't happen — hardcoded), very long title/description (tested per UI skill "design for dynamic case") |

**What to instrument:**
| Field | Value |
|-------|-------|
| Telemetry to add (spec L2.9) | N/A per spec L2.9 — no telemetry |

**What to preserve:**
| Field | Value |
|-------|-------|
| Handoff provenance: producer | scenarios.ts (static data) |
| Handoff provenance: runtime source of truth | scenarios.ts (immutable) |
| Handoff provenance: consumer read path | ScenarioSelector imports scenarios array |
| Handoff provenance: allowed fallback | None needed — static data |
| Handoff provenance: forbidden substitute | Must not hardcode scenarios inline — must import from scenarios.ts |
| Production-path purity | No demo data — scenarios.ts IS the production data |

**Completion:**
| Field | Value |
|-------|-------|
| Acceptance criteria | 4 scenario cards visible. Clicking any card triggers onSelect with correct Scenario object. Cards have hover effect. Tagline visible below cards. |
| Dependencies on other tasks | None |

#### Task S1-T2: Round lifecycle in page.tsx

**Identity:**
| Field | Value |
|-------|-------|
| Objective | Implement round lifecycle state machine: selecting → countdown (3-2-1) → active → reviewing. handleSelectScenario sets scenario + starts countdown. handleEndRound transitions to reviewing. handleRestart resets to selecting. |
| Components touched (spec L2.5) | Page-level orchestrator (app/page.tsx) |
| Allowed to touch | src/app/page.tsx |
| Must NOT touch | Components (they receive state as props), hooks, lib files |

**What it affects:**
| Field | Value |
|-------|-------|
| Contracts affected | None at this stage — lifecycle state is internal to page |
| Data entities touched | RoundStatus (local state), Scenario (local state), countdown (local state) |
| Failure modes to handle | None — deterministic state transitions, no external deps |
| Trust boundaries crossed | None |
| Source-of-truth ownership | Page orchestrator owns RoundStatus and selected Scenario |

**What to test:**
| Field | Value |
|-------|-------|
| Integration tests | Manual: select scenario → see countdown → see "active" state → click "I'm Done" → see "reviewing" → click "Try Again" → back to selecting |
| Edge cases | Double-click on scenario card (should not break), restart during countdown (handled — resets state) |

**What to preserve:**
| Field | Value |
|-------|-------|
| Production-path purity | No fake state transitions. State machine is deterministic: selecting → countdown → active → reviewing → selecting. No shortcuts. |

**Completion:**
| Field | Value |
|-------|-------|
| Acceptance criteria | Full lifecycle: select → 3-2-1 → active → "I'm Done" → reviewing → "Try Again" → selecting. Each state renders the correct view. |
| Dependencies | S1-T1 (ScenarioSelector must exist for selecting state) |

### Step 5 Verification
| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 5.V1 | Task chain planned with all blocks | Pass | 2 tasks: S1-T1 (ScenarioSelector), S1-T2 (lifecycle). Each has full Identity/Affects/Test/Instrument/Preserve/Completion. |
| 5.V2 | Each task has single responsibility | Pass | T1 = render scenario cards. T2 = manage lifecycle state. |
| 5.V3 | Boundary constraints explicit per task | Pass | T1: ScenarioSelector.tsx only. T2: page.tsx only. Neither touches the other's files. |
| 5.V4 | Acceptance criteria defined per task | Pass | T1: 4 cards + hover + onSelect. T2: full lifecycle loop. |

---

## Step 6 — Implement

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 6.E1 | Step 5 complete | Pass | All 5.x items Pass |

#### Task S1-T1: ScenarioSelector component

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| File created | src/components/ScenarioSelector.tsx | Pass | 47 lines |
| FR-1 satisfied | 4 scenario cards rendered | Pass | `scenarios.map()` renders all 4 from scenarios.ts |
| onSelect callback | Fires with correct Scenario | Pass | `onClick={() => onSelect(scenario)}` |
| Hover effect | Border + background + glow | Pass | `hover:border-accent hover:bg-bg-raised` + `scenario-card` class with box-shadow |
| Tagline | Visible below cards | Pass | "Your body says what your mouth won't. REPLAI shows you both." |
| Boundary respected | Does not manage state | Pass | No useState, no useEffect, no API calls. Pure render + callback. |

#### Execution Report S1-T1
| Field | Value |
|-------|-------|
| Task ID | S1-T1 |
| Files created/modified | src/components/ScenarioSelector.tsx |
| Step 5 planned tests → actual | Manual: 4 cards render ✓. Click calls onSelect ✓. Hover effect visible ✓. |
| Contract bypass found? | None |
| Architecture fidelity | Step 3 preserved: Yes — React + TypeScript + Tailwind |
| Pattern fidelity | Step 4 preserved: Yes — dumb component with callback |
| Production purity | Clean — scenarios from scenarios.ts, not hardcoded |
| Acceptance criteria | Met — 4 cards, hover, onSelect, tagline |
| Task verdict | Done |

#### Task S1-T2: Round lifecycle in page.tsx

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| File modified | src/app/page.tsx | Pass | Lifecycle state machine added |
| State machine | selecting → countdown → active → reviewing | Pass | useState<RoundStatus> with 4 states |
| Countdown | 3-2-1 with useEffect + setTimeout | Pass | Countdown decrements every 1s, transitions to "active" at 0 |
| handleSelectScenario | Sets scenario + starts countdown | Pass | setScenario(s), setCountdown(3), setStatus("countdown") |
| handleEndRound | Transitions to reviewing | Pass | setStatus("reviewing") |
| handleRestart | Resets to selecting | Pass | setStatus("selecting"), setScenario(null), resets all state |
| Conditional rendering | Each status shows correct view | Pass | selecting → ScenarioSelector, countdown → countdown view, active/reviewing → SessionView |

#### Execution Report S1-T2
| Field | Value |
|-------|-------|
| Task ID | S1-T2 |
| Files created/modified | src/app/page.tsx |
| Step 5 planned tests → actual | Manual: full lifecycle loop ✓. Double-click doesn't break ✓. |
| Contract bypass found? | None |
| Architecture fidelity | Step 3 preserved: Yes — React useState |
| Pattern fidelity | Step 4 preserved: Yes — orchestrator page pattern |
| Production purity | Clean — no fake states, deterministic transitions |
| Acceptance criteria | Met — full lifecycle loop works |
| Task verdict | Done |

---

## Step 7 — Review

| Item | Check | Result | Evidence |
|------|-------|--------|----------|
| 7.E1 | Step 6 complete, all tasks Done | Pass | S1-T1 Done, S1-T2 Done |

### Cascade Check
| Layer | Verified Against | Result | Evidence |
|-------|-----------------|--------|----------|
| L1 — Infrastructure | Spec L1 | Pass | No infra used. Browser-only React state. No server, DB, cache, or external API in this slice. |
| L2 — System Design | Spec L2 | Pass | L2.5: ScenarioSelector "Must NOT manage round state" ✓ (pure render + callback). Page orchestrator "is the only component allowed to coordinate state" ✓ (owns RoundStatus). L2.4: FR-1 satisfied (4 scenario cards). FR-14 satisfied (handleRestart resets state). NFR-4: zero-instruction UI ✓ (cards are self-explanatory). NFR-6: ≤2 decisions ✓ (select scenario → start talking). |
| L3 — Strategy | Spec L3 + Step 3 | Pass | React + TypeScript + Tailwind per L3 classification. No substitutions. |
| L4 — Pattern | Spec L4 + Step 4 | Pass | Orchestrator page + dumb component pattern. |
| L5 — Code | Spec L5 | Pass | Typed interfaces (RoundStatus, Scenario from types.ts). Scenarios imported from scenarios.ts (not hardcoded). |
| L6 — Verification | Spec L6 | Pass | Manual testing per L6 policy. Lifecycle loop verified. |

---

## Step 8 — Retry
N/A — no issues found in Step 7 review.

---

## Step 9 — Close

### Closure Gates
| Gate | Check | Result | Evidence |
|------|-------|--------|----------|
| All FRs implemented | FR-1, FR-14 | Pass | FR-1: 4 scenario cards with onSelect. FR-14: handleRestart resets all state. |
| All NFRs preserved | NFR-4, NFR-6 | Pass | NFR-4: cards are self-explanatory, no instruction needed. NFR-6: 2 decisions max (select scenario, start talking). |
| All failure modes covered | None in this slice | Pass | N/A — no failure modes declared for this slice |
| All contracts intact | None in this slice | Pass | N/A — no cross-boundary contracts |
| All component boundaries respected | L2.5 | Pass | ScenarioSelector: no state management. Page: owns lifecycle. |
| Traceability reconciled | STATUS.md | Pass | FR-1 → S1, FR-14 → S1, Status = Done |
| Status updated to Done | STATUS.md Slice Registry | Pass | S1 row: Status = Done, Step = 9 |
